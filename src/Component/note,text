

<div className="w-full flex-1/2 flex flex-col gap-7">
  <h1 className="font-bold text-[30px]">{reviews[active].title}</h1>
  <div className="flex justify-start items-center gap-1">
      {Array.from({ length: reviews[active].rating }).map((_, index) => (
          <Star key={index} />
      ))}
  </div>
  <p className="text-base text-accent max-w-[500px]">
      {reviews[active].description}
  </p>
</div>

